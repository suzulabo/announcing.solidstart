{
  "name": "@announcing/types",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "main": "src/index.ts",
  "engines": {
    "node": "=16"
  },
  "dependencies": {
    "ajv": "^8.11.2",
    "ajv-formats": "^2.1.1"
  },
  "devDependencies": {
    "vitest": "^0.26.2"
  }
}
